// mathOperations.js

// Function to add two numbers
function add(a, b) {
  return a + b;
}

// Function to subtract two numbers
function subtract(a, b) {
  return a - b;
}

// Exporting functions as properties of an object
module.exports = {
  add,
  subtract,
};
